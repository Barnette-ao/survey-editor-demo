我现在要测试logicRules的数据结构是否符合期待。

插入操作是否真的能将logicRule保存进questionSettings.logicRules

跳转逻辑中要加入正常结束和提前结束两项

先实现，插入新元素，删除旧元素和修改旧元素的事情吧
插入新元素
前提条件：插入的逻辑规则不是默认否认逻辑规则，不是填写到一半的逻辑规则，或者可以给个判断，如果是编写到一半的逻辑规则，不会被插入json字符串，但是会被保存入questionSettings.logicRules
要实现这一步需要一个完整性检查

但是现在我不想考虑，有只填写一半的情况。
先处理两种情况，插入默认的跳转|显示的逻辑规则
2.插入完整的逻辑规则

要从中筛选出新插入的填写完整的逻辑规则，
默认的直接删去，不能加入logicRules

保存按钮，发现新插入的逻辑规则，然后判断是否是默认的逻辑规则，就不加入logicRules

