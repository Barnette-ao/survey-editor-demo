bug1:
插入新元素，删除元素，以及复制元素，都是会重新将所有题目元素的name属性重新赋值的。
而逻辑规则最后的设置项中，极端依赖于name属性。
如果name属性变了，那么逻辑规则也会跟着失效或者错乱。

bug2:
删除有多个逻辑的题目，删除该题目之后，该题目的逻辑只删除了一个，还有一个逻辑没有被删除

待测试任务：
1.删除有单个逻辑规则。
2.删除有多个逻辑规则的题目的某一条规则。
3.删除有多个逻辑规则的题目的所有规则。
4.修改有单个逻辑规则的题目的逻辑规则。
5.修改有多个逻辑规则的题目的某一条规则。
6.修改有多个逻辑规则的题目的所有规则。

