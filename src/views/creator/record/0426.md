['html', 'panel', 'signaturepad', 'multipletext', 'file', 'ranking', 'matrix', 'text', 'comment']

getLogicRuleElement(ruleIndex, index)

静态的，
动态的，


拖拽后位置不对的问题并没有解决。

先解决拖动题目之后，逻辑规则并不能插入到正确的位置的问题。

然后要解决，第一个题目简介题目应该是不可以拖动的。这个并没有实现。

昨天总共有两个问题
1.拖动之后，拖动按钮会失效，不能再次拖动
2.拖动之后，逻辑规则没有插入到正确的位置。

现状
1.好像解决了
2.没有解决。

拖动位置不对。
分不同的情况
1. 同页面的题目之间的拖拽。
2. 不同页面的题目之间的拖拽。

拖动的核心思想：
从原来的page中删除在拖拽的题目，然后获取到插入位置的索引，然后插入到新的位置。

主要代码
```js
questionSettings.pages[fromPageIndex].elements.splice(fromQuestionIndex, 1)

questionSettings.pages[toPageIndex].elements.splice(toQuestionIndex, 0, question)
```

页码要重新计算，以前用name，现在要用number。

要保持结构的统一，当简介元素更新时，监听到更新，然后生成一个对应的对象，插入到questionSettings.pages[0].elements[0]，然后保存到数据库，取出来最新的questionSettings又要调整它的结构。就是这样。questionSettings上不会有一个instructionElement属性来保存简介元素对象

我在调整页面拖拽的bug的时候，发现最好分页组件要和第一页的页码组件分开。

html结构是
```html
<instruction :element = "questionSettings.pages[0].elements[0]" />

<div class="pages">
	<page v-for="(page, index) in withoutInstruction :key="index" :page="page" :index="index" />
</div>
    
```

questionSettings.pages.length > 1 页码才显示