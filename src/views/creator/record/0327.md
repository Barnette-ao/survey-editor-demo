测试出的问题有：

点击保存，logicRules插入两条规则，这不合适

插入的一条中有一条是默认的跳转逻辑，这一条不应该加入的

必须在classifyHistory函数中将其删去

做完基本的测试之后，我一定要删除logicRules.ifConditions中每一个元素的element属性，因为它完全多余

reactive包装的对象会用Proxy包装一个对象返回，如果删除该对象的一个属性，不会影响到原来的对象。因为Proxy包装的对象和原来的对象不是一个引用

解构删除某一个属性
const {待删除的属性，rest} = testObj
testObj = rest
这样也是改变了testObj的引用，rest的引用和原来的testObj的引用不一样，rest是重新声明的一个对象，然后现在第一行的testObj的引用与rest是不同的。所以改变不了原来的testObj

bug
新添加的