不要添加逻辑设置这个按钮，那么当某一个题目想要一次性设置多条逻辑规则的时候，就不方便了。如果一点开逻辑设置对话框，就有一个默认的规则，但只有一个默认的规则。当我设置完一条规则之后，我再想设置第二条规则，我怎么添加这第二条规则呢？

显示逻辑中不用设置添加逻辑按钮，跳转逻辑要保留添加逻辑按钮，在点开逻辑设置对话框时，如果没有显示的逻辑规则，那么就


测试，自定义的属性是否可以给在JSON编辑器中显示

定义的方式是错的，不能正确的在JSON编辑器中显示

现在