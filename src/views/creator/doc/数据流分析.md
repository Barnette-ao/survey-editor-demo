编辑器页，预览页，JSON编辑页

编辑器页

变量：qustionSettings
初始值：{} : TS语法报错，错误的静态结构，空的数据结构好过空对象 >
      storageService.loadForRuntime : > 
      afterGetInitialSettings >
      debounce( storageService.saveRuntimeSettings ,1000)

这种只适合单人编辑同一份问卷，如果要切换到另一份问卷呢，好像没有办法
好像在直接打开问卷编辑器之前需要有一个页面，即让用户选择是否继续编辑上一次编辑过的问卷，还是选择新建另一个问卷。
我虽然设计了相关的修改activeSurveyId的函数，但是我并没有地方让用户调用它
这算是一种设计缺陷吧